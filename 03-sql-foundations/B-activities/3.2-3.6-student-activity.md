# Activity 3.2 - 3.6
1. Create a new database called 'users_account_db'
2. creat a table in your new database called 'users', with the following columns below:
  - user_id: with a datatype of int, which should be auto generated.
  - firstname
  - lastname
  - email
  - age
  - date_joined
  - gender
  - state
  - country
    Please note: assign the right data type for each column you specify.

3. insert 10 different user records inside the new table you created, with different 'date_joined' and 'state' value.
4. Write the SQL command to retrieve all records in the table.
5. Fetch all users whose 'user_id' is greater than 3
6. Fetch the top 4 users in the table
7. Fetch all male users
8. Fetch the oldest user(s) in th table
9. Fetch all uses and sort by age
10. Show me the count of all users in the table
11. Fetch all users whose age is greater than 10 and is female

