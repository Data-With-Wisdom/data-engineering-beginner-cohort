## Quick Quiz

1. In ETL, where does transformation happen?
2. In ELT, what lands in the staging area?
3. Do analysts ever query raw data in ELT?
4. What does CDC stand for and what does it do?
5. What does Zero-ETL automate?
6. Does Zero-ETL eliminate the need for transformation?
7. What is the main limitation of Zero-ETL?
















<!-- 

**Answers:**
1. Outside the warehouse (in a separate tool like Python/Spark/SQL/DBT)
2. Raw, messy data — an exact copy from the source
3. No — they only query the clean analytics layer
4. Change Data Capture — it reads database transaction logs 
   and captures every change (INSERT, UPDATE, DELETE) in real-time
5. The Extract and Load steps (via CDC)
6. No — transformation (SQL/dbt) is still your responsibility
7. Vendor specific — only works within the same cloud ecosystem -->

