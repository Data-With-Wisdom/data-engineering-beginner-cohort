## Activity 5.2

**Task:** Load data from different formats.

1) Load the CSV:

```python
import pandas as pd

df_csv = pd.read_csv("datasets/employees.csv")
print(df_csv.head())
```

2) Load the JSON:

```python
import pandas as pd

df_json = pd.read_json("datasets/employees.json")
print(df_json.head())
```

3) Confirm both have the same columns:

```python
print(df_csv.columns)
print(df_json.columns)
```


